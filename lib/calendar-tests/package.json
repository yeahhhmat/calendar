{
  "name": "tests",
  "version": "0.0.0",
  "private": true,
  "comments": [
    "notice: future release testing",
    "mocha & chai test suites for ../calendar/*",
    "imported via relative path for future release testing"
  ],
  "scripts": {
    "test": "mocha",
    "local:build-calendar": "cd ../calendar && npm run build:dev",
    "local:test": "npm run local:build-calendar && npm t",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix"
  },
  "devDependencies": {
    "chai": "^4.3.6",
    "eslint": "^8.18.0",
    "mocha": "^10.0.0"
  },
  "dependencies": {
    "@whilethiscompiles/calendar": "file:../calendar",
    "dayjs": "^1.11.3"
  }
}
